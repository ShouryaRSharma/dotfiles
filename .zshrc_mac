# ======================
# ~/.zshrc.mac
# ======================
# Load secret variables
source ~/.secret_vars

# Kubeconfigs aggregated from multiple files
export KUBECONFIG=$(find ~/.kube/ekscfg/kubeconfigs -type f | paste -s -d: -)

# Set basic TMUX theme for Mac
export TMUX_THEME='basic'
